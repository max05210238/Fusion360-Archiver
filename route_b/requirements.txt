# Route B dependencies — install with:  pip install -r requirements.txt
requests>=2.28   # HTTP calls to the Autodesk APS (Data Management) API
flask>=2.2       # serves the local web UI at http://localhost:8080
